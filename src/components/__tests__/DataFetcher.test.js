import React from 'react'
import { cleanup } from '@testing-library/react'
import DataFetcher from '../DataFetcher'

afterEach(cleanup)

it('should ...', () => {
  expect(<DataFetcher />).toBeTruthy()
})